@page
@model IndexModel
@{
	ViewData["Title"] = "Home page";
}

<div class="text-center">
	<h1 class="display-4">RSS Viewer</h1>
	<table>
		<tr>
			<th>Title</th>
			<th>PublicationDate</th>
		</tr>
		@foreach (var item in Model.RSSViewModels)
		{
			<tr>
				<td>
					@item.Title
				</td>
				<td>@item.PublicationTime.ToString("dd-MM-yyyy")</td>
			</tr>
		}
	</table>
</div>
